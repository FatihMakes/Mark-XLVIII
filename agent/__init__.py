# agent package — MARK XXXIX autonomous task executor (planner / executor / queue).
# Explicit __init__ so this local package wins over any unrelated 'agent' package
# that may be installed in site-packages (Python prefers a regular package on an
# earlier sys.path entry over a later one).
